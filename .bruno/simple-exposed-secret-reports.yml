info:
  name: Get Simple Exposed Secret Reports
  type: http
  seq: 7

http:
  method: GET
  url: "{{endpoint}}/api/exposed-secret-reports/simple"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
